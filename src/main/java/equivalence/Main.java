package equivalence;

import equivalence.judgement.core.ProgramClassifier;

public class Main {
    public static void main(String[] args) {
        ProgramClassifier classifier = new ProgramClassifier(args[0]);
    }
}